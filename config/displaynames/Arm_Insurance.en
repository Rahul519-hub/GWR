<?xml version="1.0"?>
<Entity
  name="Arm_Insurance">
  <Columns>
    <Column
      beanPath="Arm_Insurance.FirstName"
      name="Fname"/>
    <Column
      beanPath="Arm_Insurance.LastName"
      name="Lname"/>
    <Column
      beanPath="Arm_Insurance.Gender"
      name="Gender"/>
  </Columns>
  <DisplayName>if (Gender == GenderType.TC_M){
  return "Mr" + Fname+" " +Lname
}
else if(Gender == GenderType.TC_F){
  return "Miss" + Fname+" " +Lname
}else{
  return Fname+" " +Lname
}</DisplayName>
</Entity>